<%= javascript_include_tag "https://www.gstatic.com/charts/loader.js" %>
<%= javascript_include_tag "chartkick" %>

<%= pie_chart @products.group(:second_hand).count,donut: true %>
<%= pie_chart @products.group(:brand).count %>


<%= line_chart @products.group_by_day(:purchase_date).count, messages: {empty: "No data"}%>

<%= bar_chart @materials%>

<%= pie_chart @materials.group(:name).count %>


<!-- test : -->
pie_chart @proportion.group(:percentage).sum('material.name')


<!-- Rappel pour joined tables : -->
<% @cloth.proportions.each do |proportion| %>
            <tr>
              <td><%= proportion.material.name %></td>
              <td><%= proportion.percentage %></td>
            </tr>
          <% end %>


